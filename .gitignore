/maggie
/dist/
*.log
.DS_Store

# Node
frontend/node_modules/

# Docker — local snapshots (jsonl is a throwaway import, not source)
snapshot/*.jsonl

# Exported DB backups (team data; pushed to a separate backup repo, not this one)
backups/
