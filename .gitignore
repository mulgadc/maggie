/maggie
*.log
.DS_Store
.env
*.out

# Vite build output, embedded into the binary by go:embed. Build artefact, not
# source — `make build` regenerates it when missing.
cmd/maggie/web/

# Node
frontend/node_modules/
frontend/.tanstack
frontend/*.tsbuildinfo
frontend/.pnpm-store
frontend/coverage

# Docker — local snapshots (jsonl is a throwaway import, not source)
snapshot/*.jsonl

# Exported DB backups (team data; pushed to a separate backup repo, not this one)
backups/
